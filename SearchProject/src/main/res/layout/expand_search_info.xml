<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/main_background" >

    <TextView
        android:id="@+id/tv_expand_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="TextView"
        android:textColor="@color/black"
        android:textSize="23sp" />

    <TextView
        android:id="@+id/tv_expand_contact"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/tv_expand_title"
        android:layout_marginTop="18dp"
        android:autoLink="email|phone"
        android:text="TextView"
        android:textColor="@color/black"
        android:textSize="23sp" />

    <TextView
        android:id="@+id/tv_expand_content"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_expand_map"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/tv_expand_contact"
        android:layout_marginTop="18dp"
        android:autoLink="all"
        android:text="TextView"
        android:textColor="@color/black"
        android:textSize="23sp" />

    <Button
        android:id="@+id/btn_expand_map"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="18dp"
        android:text="@string/btn_open_map" />

</RelativeLayout>